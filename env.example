# --- Google Sheets ---
# Spreadsheet ID (из URL таблицы)
GOOGLE_SHEET_ID=

# Имя листа (вкладки), например "Plan"
GOOGLE_SHEET_TAB=Plan

# service_account | oauth_user
GOOGLE_AUTH_MODE=service_account

# Путь к JSON ключу сервис-аккаунта (не коммитить реальный файл)
GOOGLE_SERVICE_ACCOUNT_FILE=secrets/service-account.json

# (опционально) scopes через запятую
GOOGLE_SCOPES=https://www.googleapis.com/auth/spreadsheets.readonly

# Диапазон для чтения (пример). Подстроим под реальную структуру.
GOOGLE_SHEET_RANGE=A:Z

# --- Telegram (уведомления/бот) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Weekly report (3rd working day = Wednesday) ---
# enable sending weekly audit report to TELEGRAM_CHAT_ID
WEEKLY_REPORT_ENABLED=1
# local time HH:MM
WEEKLY_REPORT_TIME=10:00
WEEKLY_REPORT_TZ=Europe/Moscow

# --- Persistent issues (not fixed N days) ---
# where to store issue history (json). default: <LOCAL_XLSX_OUTPUT_DIR>/issue_history.json
ISSUE_HISTORY_PATH=
# show red issues older than N days
PERSISTENT_RED_DAYS=14

# --- Local XLSX source (for audit-xlsx and bot) ---
# "auto" = take first .xlsx in the project folder
LOCAL_XLSX_PATH=auto
# optional sheet name (default: first sheet)
LOCAL_XLSX_SHEET=
# where to put fixed copies
LOCAL_XLSX_OUTPUT_DIR=out

# --- OpenAI (optional, for GPT-5 summaries) ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5

# --- Поведение аудита ---
# Сколько дней без обновлений считать проблемой
STALE_DAYS=3

